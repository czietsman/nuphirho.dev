

```json
{
  "bugs": [
    {
      "description": "Loop condition `while low < high` should be `while low <= high`. When low == high, the last remaining element is never checked, so the function returns -1 even if that element is the target.",
      "line": "while low < high:"
    }
  ],
  "verdict": "buggy"
}
```
